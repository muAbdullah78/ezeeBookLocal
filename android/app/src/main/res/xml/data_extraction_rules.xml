<?xml version="1.0" encoding="utf-8"?>
<!--
  EzeeBook stores every customer, order and measurement only on the device.
  Excluding everything keeps that promise on Android 12+, where cloud backup
  and device-to-device transfer would otherwise copy the SQLite database off
  the phone. Tailors move their data deliberately, via Settings > Backup.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
